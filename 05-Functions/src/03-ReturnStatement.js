// return statement => the return statement ends the execution of a function and send the value back to place where the function was called 

function addNumbers(a,b){
    return a + b; 
}

let result = addNumbers(5,4);
console.log(`Result is ${result}`);