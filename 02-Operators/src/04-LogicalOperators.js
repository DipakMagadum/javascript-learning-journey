let isLoggedIn = true;
let hasAdminAccess = false;

console.log(isLoggedIn && hasAdminAccess); //  AND && both condition must be true

let isWeekend = true;
let isHoliday = false;

console.log(isWeekend || isHoliday); // OR || atleast one condition must be true

let isAccountBlocked = false;

console.log(!isAccountBlocked); // NOT ! flip the boolean true becomes false and false becomes true