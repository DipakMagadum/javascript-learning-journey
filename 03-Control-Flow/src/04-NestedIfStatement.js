// // syntax
// if (condition1) {

//     if (condition2) {
//         // code
//     }

// }

let isLoggedIn = true;
let hasProfile = true;

if (isLoggedIn) {
    if (hasProfile) {
        console.log("Profile is ready.");
    }
}